server:
  port: 9091
spring:
  application:
    name: CloudApiGateway
  cloud:
    gateway:
      routes[0]:
        id: user-list
        uri: lb://KeycloakResourceServer
        predicates[0]: Path=/users
        predicates[1]: Method=GET
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9092/eureka


